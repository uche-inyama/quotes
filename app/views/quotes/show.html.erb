<%= turbo_stream_from "line_item_dates"%>
<div>
  <%= link_to sanitize("&larr; Back to quotes"), quotes_path %>
  <div class="header">
    <h1>
      <%= @quote.name %>
    </h1>
    <%= turbo_frame_tag dom_id(@quote, :total_top) do%>
        <%= render "quotes/total_top", quote: @quote %>
    <% end %>
  </div>

  <%= link_to "New date", new_quote_line_item_date_path(@quote), 
        data: {turbo_frame: dom_id(LineItemDate.new)}, 
        class: "btn btn--primary" %>

  <%= turbo_frame_tag LineItemDate.new %>

  <%= turbo_frame_tag "line_item_dates" do%>
    <%= render @line_item_dates, quote: @quote %>
  <% end %>
</div>
<%= turbo_frame_tag dom_id(@quote, :total) do%>
  <%= render "quotes/total", quote: @quote %>
<% end %>